## Weather App

This app uses google geolocation api to get the weather for a especific location (address, city).

To get started you need to download/clone the project, after that you need to run:

```
npm install
```